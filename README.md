The OpenGL file contains necessary libraries. To run the code on VSCode importing the libraries is important. 
Export the OpenGl in a folder and keep the .py files also in that folder.
Open the folder in VSCode and run.
Happy coding.
